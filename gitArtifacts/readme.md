# GitArtifacts

```commandline
usage: gitArtifacts.py [-h] [-v] {get,make} ...

positional arguments:
  {get,make}     Commands
    get          Get Artifacts
    make         Make Artifacts

optional arguments:
  -h, --help     show this help message and exit
  -v, --verbose  verbose (debug) logging

```
